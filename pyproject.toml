[project]
name = "wanderai"
version = "0.1.0"
description = "Scene-agnostic egocentric object-search RL environment"
requires-python = ">=3.10"
dependencies = ["numpy", "certifi"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools.packages.find]
include = ["wanderai*"]
